# Eleos Task - Solution Proposal
## by Amitay Drummer
### Installation and running:
1. run `pip install -r requirements.txt`
2. run `python main.py` - it will run both services on two separate processes
